# Phase 18: Frontend Dashboard Shell

Phase 18 adds a Next.js dashboard shell.

Routes:

- `/reviews`
- `/reviews/[id]`
- `/hitl`
- `/trace`
- `/economics`

The frontend currently uses mock data through `src/lib/api.ts`, so it runs without backend credentials.

