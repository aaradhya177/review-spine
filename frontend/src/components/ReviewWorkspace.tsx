"use client";
import { Check, ChevronDown, FileCode2, MessageSquareWarning, ShieldCheck, X } from "lucide-react";
import { useState } from "react";
import type { ReviewRow } from "@/lib/mockData";
import { Severity, StatusBadge } from "./StatusBadge";

const codeLines = [
  ["84", "84", "  const session = await sessions.get(token);"],
  ["85", "85", "  if (!session) return unauthorized();"],
  ["86", "86", ""],
  ["87", "87", "+ const user = await users.find(session.userId);"],
  ["88", "88", "+ return issueToken({ user, refresh: true });"],
  ["89", "89", "  return issueToken({ user, refresh: true });"],
];

export function ReviewWorkspace({ review }: { review: ReviewRow }) {
  const [selectedId, setSelectedId] = useState(review.findings[0]?.id); const [decision, setDecision] = useState<string | null>(null);
  const selected = review.findings.find((finding) => finding.id === selectedId) ?? review.findings[0];
  const files = Array.from(new Set(review.findings.map((finding) => finding.file))).concat(review.findings.length ? ["backend/app/models/session.py"] : []);
  return <section className="panel"><div className="review-summary"><div><h3 className="summary-title">Review summary</h3><div className="summary-meta"><StatusBadge status={decision ?? review.status} /><span><strong>{review.findings.length}</strong> open findings</span><span>Analyzed in 42s</span><span>2 agents completed</span></div></div><div className="summary-actions"><button className="button" type="button" onClick={() => setDecision("dismissed")}><X className="icon" />Dismiss all</button><button className="button primary" type="button" onClick={() => setDecision("approved")}><Check className="icon" />Approve review</button></div></div><div className="review-layout"><aside className="review-sidebar"><div className="subpanel-header"><h3>Changed files</h3><span>{files.length}</span></div><div className="file-list">{files.map((file, index) => <button className={`file-item ${index === 0 ? "selected" : ""}`} type="button" key={file}><FileCode2 className="file-icon" /> <span>{file.split("/").pop()}</span>{index < review.findings.length && <span className="file-count">{review.findings[index] ? 1 : 0}</span>}</button>)}</div></aside><div className="review-main"><div className="review-tabs"><button className="review-tab active" type="button">Changes</button><button className="review-tab" type="button">Summary</button><button className="review-tab" type="button">Checks <span className="muted">2</span></button></div><div className="diff-toolbar"><span className="mono">{files[0] ?? "No changed files"}</span><span>1 of {files.length} files</span></div><div className="diff-code">{codeLines.map(([oldLine, newLine, code]) => <div className={`code-line ${code.startsWith("+") ? "added" : ""} ${newLine === String(selected?.line) ? "marked" : ""}`} key={`${oldLine}-${newLine}`}><span className="line-no">{oldLine}</span><span className="line-no">{newLine}</span><code className="code-text">{code}</code></div>)}</div></div><aside className="findings-panel"><div className="subpanel-header"><h3>Findings</h3><span>{review.findings.length} open</span></div><div className="finding-list">{review.findings.length ? review.findings.map((finding) => <button className={`finding ${finding.id === selectedId ? "selected" : ""}`} type="button" key={finding.id} onClick={() => setSelectedId(finding.id)}><div className="finding-heading"><Severity value={finding.severity} /><strong>{finding.summary}</strong></div><p>{finding.category.replaceAll("-", " ")} · {finding.agent} agent</p><div className="finding-meta"><span className="mono">{finding.file}:{finding.line}</span><span>{Math.round(finding.confidence * 100)}% confidence</span></div></button>) : <div className="empty"><div><ShieldCheck className="icon" /><strong>Looks clear</strong><span>No actionable findings.</span></div></div>}</div>{selected && <div className="finding-detail"><div className="detail-heading"><h4>{selected.summary}</h4><button className="button ghost" type="button" aria-label="Dismiss finding"><X className="icon" /></button></div><div className="detail-section"><div className="detail-label">Why this matters</div><p>The refreshed token path can return a session before the role check runs. A caller with an expired role may retain access longer than intended.</p></div><div className="detail-section"><div className="detail-label">Suggested next step</div><p>Add the role assertion before issuing the refreshed token and cover the denial path with a regression test.</p></div><div className="detail-section"><div className="detail-label">Review decision</div><div className="actions"><button className="button primary" type="button" onClick={() => setDecision("finding resolved")}><Check className="icon" />Resolve</button><button className="button" type="button"><MessageSquareWarning className="icon" />Comment</button></div></div></div>}</aside></div></section>;
}
